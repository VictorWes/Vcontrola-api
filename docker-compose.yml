version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: vcontrola-db
    environment:
      POSTGRES_DB: vcontrola
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5430:5432"